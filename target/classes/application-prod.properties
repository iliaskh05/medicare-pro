spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.flyway.enabled=true
spring.flyway.validate-on-migrate=true
logging.level.com.crm.medicare=INFO
# Secrets obligatoires via variables d'environnement :
# RADIOCRM_JWT_SECRET, DB_URL, DB_USERNAME, DB_PASSWORD, MAIL_USERNAME, MAIL_PASSWORD
radiocrm.cors.allowed-origins=${RADIOCRM_CORS_ORIGINS}
