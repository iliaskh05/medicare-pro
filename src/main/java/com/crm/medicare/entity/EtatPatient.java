package com.crm.medicare.entity;

/**
 * État du patient dans le parcours — valeurs JSON exactes du contrat Worklist.
 */
public enum EtatPatient {
    attendu,
    arrive,
    retard,
    attente_longue
}
